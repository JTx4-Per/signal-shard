# Pinned runtime + test dependencies for email_intel v1.
# Generated from the working .venv where 234/234 tests pass, mypy --strict is
# clean, and ruff is clean. Regenerate with:
#   pip freeze --exclude-editable | sort > requirements.txt
# Linters (ruff, mypy) live in requirements-dev.txt.

# --- Web / ASGI ---
fastapi==0.135.3
starlette==1.0.0
uvicorn==0.44.0
httptools==0.7.1
uvloop==0.22.1
watchfiles==1.1.1
websockets==16.0
Jinja2==3.1.6
MarkupSafe==3.0.3
python-multipart==0.0.26

# --- Data / validation ---
pydantic==2.13.0
pydantic_core==2.46.0
pydantic-settings==2.13.1
annotated-types==0.7.0
typing_extensions==4.15.0
typing-inspection==0.4.2
python-dotenv==1.2.2

# --- DB / ORM / migrations ---
SQLAlchemy==2.0.49
aiosqlite==0.22.1
alembic==1.18.4
Mako==1.3.10
greenlet==3.4.0

# --- Microsoft Graph / auth ---
msal==1.36.0
cryptography==46.0.7
cffi==2.0.0
pycparser==3.0
PyJWT==2.12.1
requests==2.33.1
urllib3==2.6.3
charset-normalizer==3.4.7
certifi==2026.2.25
idna==3.11

# --- HTTP client ---
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
anyio==4.13.0

# --- Scheduler / observability ---
APScheduler==3.11.2
tzlocal==5.3.1
structlog==25.5.0

# --- Test framework (listed as runtime in pyproject per project convention) ---
pytest==9.0.3
pytest-asyncio==1.3.0
hypothesis==6.151.14
sortedcontainers==2.4.0
iniconfig==2.3.0
pluggy==1.6.0
packaging==26.0

# --- Misc transitive (kept explicit for reproducibility) ---
click==8.3.2
PyYAML==6.0.3
Pygments==2.20.0
annotated-doc==0.0.4
librt==0.9.0
pathspec==1.0.4
